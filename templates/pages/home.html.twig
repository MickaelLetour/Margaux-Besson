{% extends "base.html.twig" %}

{% block title 'Photographe Margaux Besson' %}

{% block body %}
<div class="container-fluid row min-vh-100 m-auto" id="header">
    <div class="text-center p-0 mt-4 mx-auto text-warning col-12 col-sm-8 col-lg-6">
        <h1 class="titleLine text-warning text-center m-auto">Margaux Besson</h1>
        <h2 class="opacityTitle text-warning">Photographe à Lisbonne</h2>
        <p>(et partout dans le monde)</p>
    </div>
    <div class="align-self-center">
        <div class="wrapper m-auto col-10 col-sm-6 col-lg-8 col-xl-6">
            {% for photo in mosaique %} 
                <img class="img-fluid" src="{{ photo.title }}" alt="{{ photo.title }}">
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}